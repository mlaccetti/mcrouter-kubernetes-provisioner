---
kind: Deployment
apiVersion: extensions/v1beta1
metadata:
  name: mcrouter-provisioner
  labels:
    project: mcrouter-memcached
    role: mcrouter-provisioner
spec:
  replicas: 1
  selector:
    matchLabels:
      project: mcrouter-memcached
      role: mcrouter-provisioner
  template:
    metadata:
      labels:
        project: mcrouter-memcached
        role: mcrouter-provisioner
    spec:
      volumes:
        - name: mcrouter-provisioner-storage
          emptyDir: {}
      containers:
      - name: mcrouter-provisioner
        image: mlaccetti/mcrouter-kubernetes-provisioner:master
        imagePullPolicy: Always
        volumeMounts:
        - mountPath: "/mcrouter"
          name: mcrouter-provisioner-storage
          readOnly: false
      - name: mcrouter
        image: jamescarr/mcrouter:1.0.1
        command: ["tail","-f", "/dev/null"]
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
        volumeMounts:
        - mountPath: "/mcrouter"
          name: mcrouter-provisioner-storage
          readOnly: true
---
kind: Service
apiVersion: v1
metadata:
  name: mcrouter-service
  labels:
    project: mcrouter-memcached
    role: mcrouter-provisioner
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  selector:
    project: mcrouter-memcached
    role: mcrouter-provisioner
